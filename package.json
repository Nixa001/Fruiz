{
  "name": "merge-fruits-senegal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MERGE FRUITS SÉNÉGAL — jeu mobile casual de fusion de fruits (Phaser 3 + TypeScript + Vite + Matter)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.7.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.0"
  }
}
